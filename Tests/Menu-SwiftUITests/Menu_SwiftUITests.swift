import XCTest
@testable import Menu_SwiftUI

final class Menu_SwiftUITests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Menu_SwiftUI().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
